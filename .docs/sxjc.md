# 查询主页面：四性检测方案配置
## 表单查询条件

| **编号** | **类型** | **展示名称** |  **UI展示与交互逻辑** |

| ------ | ------ | -------- | ------------ | ------------- |

| 1      | 下拉选择器  | 检测方案名称   | 取表中检测方案名称      |

| 2      | 下拉选择器    | 检测环节   | 取FOUR_ATTR_INSPECTION值描述    |

| 3      | 下拉选择器  | 是否启用     | 默认Y           |

## 表格查询结果

### 表格工具栏按钮

| **编号** | **类型** | **按钮名称** | **UI展示与交互逻辑** |

| ------ | ------ | -------- | ------------- |

| 1  | 按钮     | 新增       | 主要按钮（点击新增空白行） |

| 2  | 按钮     | 保存       |               |

| 3  | 按钮     | 导入       |               |

| 4  | 按钮     | 导出       |               |

### 表格列

| **编号** | **类型** | **展示名称** | **UI展示与交互逻辑** |

| ------ | ------ | -------- | ------------- |

| 1      | 序号     | 序号       | 只读态           |

| 2      | 操作     | 操作       | 编辑（点击编辑当前行），详情（跳转“四性检测详情”页面）         |

| 3      | 输入框    | 检测方案名称   | 新增/编辑时必填        |

| 4      | 下拉选择器  | 检测环节     | 新增时必填，只能新增不能修改        |

| 5      | 下拉选择器  | 数据包规范   | 新增/编辑时必填        |

| 6      | 下拉选择器  | 元数据规范    | 新增/编辑时必填        |

| 7      | 开关     | 是否启用     | 默认启用           |

| 8      | 用户     | 创建人      | 只读态           |

| 9      | 日期     | 创建时间    | 只读态           |

| 10     | 用户     | 最后更新人    | 只读态           |

| 11     | 日期     | 最后更新时间   | 只读态           |


# 查询详情页：四性检测详情

## 折叠面板容器1

容器标题：真实性

### 表格工具栏按钮

| **编号** | **类型** | **按钮名称** | **UI展示与交互逻辑** |

| ------ | ------ | -------- | ------------- |

| 1 | 按钮     | 新增       | 主要按钮（点击新增空白行） |

**表格列；表格视图**

|编号|类型|展示名称|UI展示与交互逻辑|
|---|---|---|---|
|1|序号|序号|只读态|
|2|输入框|检测类型|只读态|
|3|输入框|检测编号|只读态|
|4|输入框|检测项|只读态|
|5|输入框|检测目的|只读态|
|6|输入框|检测对象|只读态|
|7|输入框|检测依据和方法|只读态|
|8|开关|是否启用|默认开|

## 折叠面板容器2

容器标题：完整性

### 表格工具栏按钮

| **编号** | **类型** | **按钮名称** | **UI展示与交互逻辑** |

| ------ | ------ | -------- | ------------- |

| 1 | 按钮     | 新增       | 主要按钮（点击新增空白行） |

**表格列；表格视图**

|编号|类型|展示名称|UI展示与交互逻辑|
|---|---|---|---|
|1|序号|序号|只读态|
|2|输入框|检测类型|只读态|
|3|输入框|检测编号|只读态|
|4|输入框|检测项|只读态|
|5|输入框|检测目的|只读态|
|6|输入框|检测对象|只读态|
|7|输入框|检测依据和方法|只读态|
|8|开关|是否启用|默认开|


## 折叠面板容器3

容器标题：可用性    

### 表格工具栏按钮

| **编号** | **类型** | **按钮名称** | **UI展示与交互逻辑** |

| ------ | ------ | -------- | ------------- |

| 1 | 按钮     | 新增       | 主要按钮（点击新增空白行） |

**表格列；表格视图**

|编号|类型|展示名称|UI展示与交互逻辑|
|---|---|---|---|
|1|序号|序号|只读态|
|2|输入框|检测类型|只读态|
|3|输入框|检测编号|只读态|
|4|输入框|检测项|只读态|
|5|输入框|检测目的|只读态|
|6|输入框|检测对象|只读态|
|7|输入框|检测依据和方法|只读态|
|8|开关|是否启用|默认开|

## 折叠面板容器4

容器标题：安全性

### 表格工具栏按钮

| **编号** | **类型** | **按钮名称** | **UI展示与交互逻辑** |

| ------ | ------ | -------- | ------------- |

| 1 | 按钮     | 新增       | 主要按钮（点击新增空白行） |

**表格列；表格视图**

|编号|类型|展示名称|UI展示与交互逻辑|
|---|---|---|---|
|1|序号|序号|只读态|
|2|输入框|检测类型|只读态|
|3|输入框|检测编号|只读态|
|4|输入框|检测项|只读态|
|5|输入框|检测目的|只读态|
|6|输入框|检测对象|只读态|
|7|输入框|检测依据和方法|只读态|
|8|开关|是否启用|默认开|

## 底部区

|编号|类型|按钮名称|UI展示与交互逻辑|
|---|---|---|---|
|1|按钮|保存|主要按钮|
|2|按钮|取消||