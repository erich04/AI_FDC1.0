# 功能需求规格模板（单功能）

> 用途：作为某个具体功能（如 `F01_Upload`）的需求规格真相来源（SSoT）。
> 原则：**可实现、可验收、可测试、可审计、可回滚**。每条规则尽量写成“输入-处理-输出-错误”。

---

## 1. 修订记录

| 版本 | 日期 | 作者 | 变更内容 | 评审人 | 备注 |
|---|---|---|---|---|---|
| v0.1 | yyyy-mm-dd | @name | 初稿 | @reviewer |  |

---

## 2. 背景与范围

### 2.1 背景（Why now）

- **业务问题**：
- **现状痛点**：
- **机会/风险**：

### 2.2 目标（Goals）

- **业务目标**：
- **用户目标**：
- **成功指标**（可量化）：（例：上传成功率、OCR准确率、审批时长）

### 2.3 非目标（Non-goals）

- **本期不做**：
- **明确不支持**：

### 2.4 相关方与使用角色

- **角色列表**：如 财务专员/复核人/管理员/审计员/外部访客
- **责任边界**：谁发起、谁审批、谁归档、谁可下载/导出

### 2.5 依赖与约束

- **上游依赖**：如 SSO、对象存储、OCR引擎、消息系统
- **下游影响**：如 报表、对账、审计导出
- **合规约束**：如 数据留存期限、脱敏、审计、访问记录

---

## 3. 术语与口径（Glossary + Definitions）

> 与 `.docs/04_Glossary.md` 保持一致；如有新增术语，在此说明并同步。

- **核心名词**：
- **关键口径**：如“金额”的币种/税含口径、“期间”的取值规则

---

## 4. 用户故事与业务流程

### 4.1 用户故事（User stories）

- **US-01**：作为……我希望……以便……
- **US-02**：

### 4.2 业务流程（Workflow）

> 用文字 + 流程图（二选一或都写），并与 `.docs/02_Workflow.md` 对齐。

- **触发条件**：
- **主流程（Happy path）**：
- **分支流程**：审批驳回/超时/撤回/重复提交
- **终态**：成功归档/失败终止/人工介入

### 4.3 状态机（State machine）

| 状态 | 含义 | 进入条件 | 退出条件 | 允许操作 | 不变量 |
|---|---|---|---|---|---|
| DRAFT |  |  |  |  |  |

---

## 5. 功能详细规格（Functional Specs）

### 5.1 业务场景与用户旅程

> 每个场景写清：前置条件、步骤、系统反馈、异常提示、产出物。

- **S-01 场景名**  
  - **前置条件**：
  - **操作步骤**：
  - **系统响应**：
  - **异常与提示**：
  - **产出**（生成的数据/文件/任务）：

### 5.2 页面/交互/原型说明（UI/UX）

> **编写层级**（自上而下，缺一不可）：**功能 → 多个页面 → 每页「页面类型 + 布局分区」→ 分区内「模块」→ 模块内「字段 / 按钮 / 交互」**。  
> 无高保真原型时，仍须用文字 + 表格把布局与交互写清；有原型/设计稿时在本节附链接或截图索引。

#### 5.2.1 页面一览（本功能涉及的全部页面）

> 先列全页面，再按编号在 **§5.2.2** 逐页展开，避免遗漏。

| 页面编号 | 页面名称 | 路由/入口（或菜单路径） | 页面类型 | 简述 | 详述 |
|---|---|---|---|---|---|
| P-01 |  | `#/...` 或「从 xx 进入」 | 列表页 / 详情页 / 新建编辑页 / 弹窗 / 抽屉 / 向导 / 嵌入式 / 其他 | 一句话说明用户来做什么 | 见下节「P-01」 |
| P-02 |  |  |  |  |  |

**页面类型说明（选用）**：列表页（筛选+表格+分页）、详情页（多区块只读/折叠）、新建编辑页（表单提交）、弹窗/抽屉（短任务）、向导（多步）、嵌入式（嵌入 iframe/子应用）等。

---

#### 5.2.2 分页面规格（每页复制一整块，编号与 §5.2.1 一致）

以下以 **P-01** 为模板；`P-02`、`P-03`… 结构相同。

##### P-01 《页面名称》

###### （1）页面类型与整体布局

- **页面类型**：（与一览表一致）
- **布局骨架**（按阅读顺序描述典型分区；名称可与设计系统对齐）：
  - **L1** 全局顶栏 / 系统标题区（若有）
  - **L2** 面包屑 / 页面标题区
  - **L3** 筛选区 / 查询区
  - **L4** 工具栏（主操作按钮、批量操作）
  - **L5** 主内容区（表格 / 表单 / 详情区块）
  - **L6** 底栏 / 分页 / 固定操作条（若有）
- **原型与设计**：Figma/蓝湖/截图路径/HTML 原型路径：  
- **响应式/适配**：（若有：最小宽度、移动端是否支持）

###### （2）分区 → 模块拆解

> **规则**：每个**布局分区**下可包含一个或多个**模块**（如「筛选表单」「按钮组」「数据表格」「详情折叠面板」）。每个模块单独用表格列字段与按钮，并写清模块间依赖（如：未选文档类型则禁用「更多筛选」）。

> **字段/按钮“来源可追溯”强制写法（让你知道字段如何来自数据模型或外部 API）**：
> - `字段逻辑说明`（字段表第 5 列）统一按以下前缀格式书写（可组合）：
>   - `DB：{表名}.{字段名}`（直接取值）
>   - `FK：{源表}.{FK字段} -> {目标表}.{目标字段}`（联表链路；多跳用 `->` 继续）
>   - `LOOKUP：{LOOKUP_TYPE}（展示字段：{name/code/...}；取值码：{字段}）`
>   - `API：{HTTP_METHOD} {资源路径}（入参：...；出参映射：... -> {字段}）`
>   - `COMPUTE：{计算/拼接/层级判断规则}`（如行政层级按 `alpha2Code` 断层决定入参）
>   - `VISIBILITY：由 {fdc_archive_rule_t}.visible_flag 决定；匹配维度：{archived_entity_unit_id/business_module_id/...}`
> - `按钮逻辑说明（含调用UI API）`（按钮表第 5 列）统一按以下前缀书写：
>   - `UI动作：{打开弹窗/跳转页面/刷新列表}`
>   - `BE/资源：{HTTP_METHOD} {资源路径}`
>   - `入参来源：{筛选字段/勾选集合/筛选快照/分页参数}`
>   - `结果处理：{成功/失败分别展示什么；是否记录审计/异步轮询}`

**〔L3 筛选区〕— 模块 M1：〈模块名称，如：默认筛选〉**

| 序号 | 字段名称 | 字段名-英文 | 类型 | 字段逻辑说明 | 备注 |
|---:|---|---|---|---|---|
| 1 | 字段示例 | Field Example | 文本 | 填写/展示逻辑说明（含校验） |  |
| 2 | 文档类型 | Document Type | 下拉单选 | 未选时禁止展开更多筛选并提示 | 默认筛选栏 |

**〔L3 筛选区〕— 模块 M2：〈更多筛选（可折叠）〉**

| 序号 | 字段名称 | 字段名-英文 | 类型 | 字段逻辑说明 | 备注 |
|---:|---|---|---|---|---|
|  |  |  |  |  |  |

**〔L4 工具栏〕— 模块 M3：〈行内/批量操作〉**

| 编号 | 按钮名称 | 按钮名称-英文 | 显示位置 | 按钮逻辑说明（含调用UI API） | 绑定权限项名称 | 新增权限项说明 |
|---:|---|---|---|---|---|---|
| 1 | 查询 | Search | 右下2 | 调用查询列表服务并刷新列表 |  |  |

##### 字段逻辑说明/按钮逻辑说明示例（建议照抄）

- 字段示例（DB）：`DB：fdc_doc_t.doc_busi_no`
- 字段示例（FK via FK）：`FK：fdc_doc_t.archived_entity_unit_id -> fdc_archived_entity_unit_t.archived_entity_id -> fdc_archived_entity_t.archived_entity_name`
- 字段示例（LOOKUP）：`LOOKUP：FDC_CARRIER_TYPE（展示字段：name；取值码：fdc_doc_t.carrier_type）`
- 字段示例（API）：`API：GET /regions/alias（入参：alpha2Code={arch_place_alpha2_code}; 出参映射：AliasChinese/AliasEnglish -> 字段）`
- 字段示例（VISIBILITY）：`VISIBILITY：由 fdc_archive_rule_t.visible_flag 决定；匹配维度：archive_rule_id 或 archived_entity_unit_id/business_module_id`
- 按钮示例（BE）：`BE/资源：POST documents/export；入参来源：filter_snapshot + {勾选doc_id集合或导出当前查询结果}；结果处理：提交成功后弹出跳转“我的导出”提示`

**〔L5 主内容区〕— 模块 M4：〈列表/表单/详情，择一或组合〉**

*列表（表格列）*

| 列名 | 字段绑定/数据来源 | 展示格式 | 排序 | 交互（链接/勾选） | 备注 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

*或表单/详情字段*

| 字段 | 控件 | 只读/可编辑 | 必填 | 校验 | 错误提示 | 备注 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

###### （3）页面级状态与通用交互

- **空态**（无数据/无权限时的文案与引导）：
- **加载态**（骨架屏/Loading 范围）：
- **错误态**（接口失败、校验失败提示位置：行内 / Toast / 弹窗）：
- **确认与危险操作**（二次确认、不可逆操作提示）：
- **页面跳转与返回**：跳转至 `P-xx` 的条件、返回是否保留筛选状态

---

##### P-02 《页面名称》

> （复制 P-01 结构填写）

---

#### 5.2.3 跨页面公共约定（可选）

- **导航关系**：页面流程图或表（从 P-a 到 P-b 的触发条件）
- **复用组件**：多页共用的筛选条、上传器、人员选择器等（行为与 §5.2.2 引用关系）
- **全局快捷键/埋点**（若有）：

#### 5.2.4 字段与校验汇总表（可选）

> 当某功能字段分散在多个模块、需要一张**总表**做评审或对接测试时使用；日常以 §5.2.2 各模块内嵌表为准。

| 字段/元素 ID | 所属页面 | 所属模块 | 类型 | 必填 | 默认值 | 校验规则 | 错误提示 | 备注 |
|---|---|---|---|---:|---|---|---|---|
|  | P-01 | M1 |  |  |  |  |  |  |

### 5.3 规则与策略（Rules）

> 尽量编号，便于引用（R-001...）。

- **R-001**：
  - **输入**：
  - **处理**：
  - **输出**：
  - **失败/错误码**：
- **R-002**：

### 5.4 异常/边界/幂等（Edge cases）

- **幂等键**：是什么、作用域（用户级/租户级）、过期时间
- **重复提交**：判定条件与处理策略
- **并发冲突**：锁/乐观版本号/重试
- **大文件/超时**：分片、断点续传、重试次数

### 5.5 与其他模块的交互

- **上游输入**：
- **下游输出**：
- **事件/消息**（若有）：topic、payload、重试、死信

---

## 6. 技术实现映射（UI - API - DB Mapping）

| 用户动作/页面 | API | DB 表/字段 | 权限点 | 日志/审计 | 备注 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

---

## 7. 数据模型设计（Data Schema）

> 与 `.docs/01_DataModel.md` 对齐；这里只写本功能新增/变更的部分，并注明迁移策略。

### 7.1 实体关系（ERD）

- **ERD 链接/图**：

### 7.2 表字典（核心表）

#### 7.2.1 新增/变更表清单

- **新增表**：
- **变更表**：
- **索引/约束**：

#### 7.2.2 字段明细

| 表 | 字段 | 类型 | NULL | 默认值 | 说明 | 口径/来源 | 索引 | 变更影响 |
|---|---|---|---:|---|---|---|---|---|
|  |  |  |  |  |  |  |  |  |

### 7.3 数据生命周期与归档

- **留存期限**：
- **归档策略**：
- **删除/匿名化**：

---

## 8. 接口规范（API Specs）

> **全局约定**（路径风格、状态码、错误体、分页等）见 `/.docs/05_API_Conventions.md`；本节只写**本功能**端点与契约。  
> **基址占位**：`FDC_URL`（与全局文档一致）。

### 8.1 单端点文档目录（每个接口建议按下列小节撰写，避免遗漏）

| 小节 | 必填内容 |
|---|---|
| **基本信息** | 用途、资源说明；是否幂等；接口版本或变更说明（若有） |
| **鉴权与权限** | 认证方式（如 Bearer）；所需 `perm_code`（见 `/.docs/03_Security.md` §2）；**数据范围**如何校验（维度与资源字段对应关系） |
| **入参** | **Path / Query / Header / Body** 分块；每参数一张表或合并表：`名称`、`类型`、`必填`、`默认值`、`说明`、`示例` |
| **出参** | **成功**：HTTP 状态 + body 结构（字段表或引用 DTO）；**失败**：与 `05` 一致的错误体（`code` / `message` / `errors`） |
| **示例** | 至少一组典型 **请求 + 成功响应**；必要时补充 **失败响应** 示例 |
| **错误码** | 业务 `code` 与 **HTTP 状态** 对照表；与 `errors[]` 中行级/字段级定位规则 |
| **幂等** | 是否要求 `Idempotency-Key` 或业务幂等键；重复提交、超时重试时的行为 |
| **限流** | 维度（用户/租户/IP/全局限流）；阈值或引用网关配置；触发 **429** 时的提示与 `Retry-After`（若有） |
| **审计** | 必须落库/日志的字段：`操作者`、`租户`、`资源类型与 ID`、`动作`、`结果`、`trace/requestId`、敏感数据是否脱敏后记录 |

> 接口数量多时，可先填 **8.2 接口总览** 再对关键路径展开 **8.3**；非核心端点可只保留总览行 + 链接至 OpenAPI/设计中心（若有）。

### 8.2 接口总览

| 资源路径（示例） | 方法 | 用途 | 鉴权/权限 | 幂等 | 限流 | 审计要点 | 备注 |
|---|---|---|---|---|---|---|---|
| `FDC_URL/...` |  |  |  |  |  |  |  |

### 8.3 关键接口定义（逐端点展开，结构对齐 8.1）

以下每个端点复制一块，将 `X` 换为序号；小节标题勿删，无内容可写「不适用」并简述原因。

#### 8.3.X `<METHOD> FDC_URL/...`

##### X.1 基本信息

- **目的**：
- **幂等性**：是 / 否；若否，说明重复调用的副作用
- **版本或变更**：（可选）

##### X.2 鉴权与权限

- **认证**：如 `Authorization: Bearer <token>`
- **权限点** `perm_code`：
- **数据范围**：与 `/.docs/03_Security.md` §3 维度如何映射到本请求资源

##### X.3 入参

**Path 参数**

| 名称 | 类型 | 必填 | 说明 |
|---|---|---:|---|
|  |  |  |  |

**Query 参数**（可选）

| 名称 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---:|---|---|
|  |  |  |  |  |

**Headers**（可选）

| 名称 | 必填 | 说明 |
|---|---:|---|
| `Authorization` | 是 |  |
| `X-Request-Id` / `traceparent` | 建议 | 链路追踪 |
| `Idempotency-Key` | 条件必填 | 创建类、导出提交等 |

**Request body**（可选）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
|  |  |  |  |

```json
{}
```

##### X.4 出参

**成功响应**（注明 HTTP 状态，如 200）

| 字段 | 类型 | 说明 |
|---|---|---|
|  |  |  |

```json
{}
```

**失败响应**（对齐 `/.docs/05_API_Conventions.md`）

```json
{
  "code": "",
  "message": "",
  "errors": []
}
```

##### X.5 示例

- **请求示例**（curl 或 HTTP 描述 + JSON）
- **成功响应示例**
- **失败响应示例**（可选）

##### X.6 错误码与 HTTP 映射

| HTTP | 业务 `code`（可选） | 场景 |
|---|---|---|
| 400 |  | 参数/校验失败 |
| 401 |  | 未认证 |
| 403 |  | 无权限 |
| 404 |  | 资源不存在 |
| 429 |  | 限流 |
| 500 |  | 服务端异常 |

##### X.7 幂等、并发与重试

- **幂等键**：
- **并发冲突**（如乐观锁、版本号）：
- **客户端重试建议**：

##### X.8 限流

- **策略**：
- **超限表现**：

##### X.9 审计与安全

- **必须记录**：（操作者、对象 ID、动作、结果、时间、requestId 等）
- **脱敏**：是否与 `/.docs/03_Security.md` §5 一致

---

## 9. 权限与安全（RBAC + Security）

> 与 `.docs/03_Security.md` 对齐；这里写本功能的具体落点。

### 9.1 权限矩阵（Role x Action）

> **角色列**须与 `/.docs/03_Security.md` →「**1. 系统角色清单**」一致：只使用已登记角色（名称或 `role_code` 与全项目统一约定一致），勿在矩阵中自拟新角色；若需新角色，先修订 `03_Security.md` 再填矩阵。

| 操作 | （角色列自 `03_Security.md` 系统角色清单复制表头） | 备注 |
|---|---|---|
|  |  |  |

### 9.2 数据分级与脱敏

- **敏感字段清单**：
- **展示脱敏规则**：（例：金额区间化、证件号掩码）
- **下载/导出规则**：

### 9.3 四性/合规检测（如适用）

- **完整性**：
- **一致性**：
- **真实性**：
- **及时性**：

### 9.4 安全控制点

- **鉴权**：会话/JWT、过期、刷新
- **授权**：租户隔离、行级权限（若有）
- **防重放**：request-id、签名（若有）
- **限流**：按用户/租户/接口

---

## 10. 消息与通知设计（Notifications）

- **触发条件**：
- **通知渠道**：站内信/邮件/IM
- **模板变量**：
- **频控/合并策略**：
- **失败重试**：

---

## 11. 可观测性与审计（Observability & Audit）

### 11.1 日志

- **关键日志点**（必须覆盖主链路与关键失败）：
- **字段规范**：`trace_id`、`tenant_id`、`user_id`、`resource_id`

### 11.2 指标（Metrics）

- **业务指标**：成功率、耗时、吞吐
- **系统指标**：CPU/内存/队列堆积

### 11.3 追踪（Tracing）

- **链路**：UI → API → DB → 外部服务

### 11.4 审计日志（Audit）

- **记录范围**：谁在何时对何对象做了什么、前后值（如适用）
- **留存期限**：

---

## 12. 非功能性要求（NFR）

### 12.1 性能与容量

- **P95/P99 延迟目标**：
- **并发目标**：
- **容量假设**：日上传量/报表页数/任务量

### 12.2 可靠性

- **SLO**：
- **降级策略**：
- **重试策略**（含退避、上限）：

### 12.3 可维护性

- **配置项**：feature flag、阈值
- **运行手册**：如何排障、如何回放

---

## 13. 数据迁移/发布/回滚（Delivery）

- **DB Migration**：新增/变更脚本、是否可逆
- **Feature Flag**：开关策略（按租户/按用户/按比例）
- **灰度计划**：
- **回滚方案**：回滚步骤、数据兼容策略

---

## 14. 测试计划（Test Plan）

> 建议把用例编号（TC-001...）用于 `tests/` 目录落地。

### 14.1 用例清单

| 用例ID | 场景 | 前置条件 | 操作 | 期望结果 | 覆盖规则/接口 |
|---|---|---|---|---|---|
| TC-001 |  |  |  |  |  |

### 14.2 回归影响

- **受影响模块**：
- **需要回归的路径**：

---

## 15. 验收标准（Acceptance Criteria）

> 用“可验证语句”写清楚：Given/When/Then 或 条款式。

- **AC-01**：
- **AC-02**：
